Creating roles
==============

`ansible-galaxy init <role_name>`

folders that aren't commited into git:
- `files`
  - files to be copied to the host
- `templates`
  - `.j2` files to be copied to the host
  - Jinja files

//vim:ft=markdown:ts=2:sw=2:sts=2:et
